# Rentry Bio 自由排版编辑器 V1

**Product Requirements Document**

---

## 一、产品概述

### 1.1 产品名称

暂定：

**Rentry Bio Editor**

名称后续可调整。

---

### 1.2 产品定位

一个面向个人用户的轻量级自由排版 Bio / Profile 编辑器。

产品主要用于制作：

* Rentry 风格 Bio
* Tumblr 风格个人简介
* 个人 Profile
* 兴趣 / 爱好介绍
* OC / Persona 介绍
* 社交平台个人介绍
* 装饰性个人长图

产品核心体验：

> **比 Photoshop / Figma / Canva 更简单，比 Rentry / 模板式 Bio 工具更自由。**

用户不需要学习专业设计软件，只需要：

**添加 → 拖动 → 调整 → 排版 → 导出。**

---

# 二、产品背景与用户问题

## 2.1 用户现有选择

用户目前可以使用：

### 专业设计软件

例如 Photoshop、Figma、Canva。

优点：

* 自由度高
* 可以自由排版

问题：

* 功能太多
* 学习成本较高
* 对只想做一个 Bio 的用户来说过于复杂

### Rentry / 类似文本页面

优点：

* 简单
* 写内容方便
* 不需要复杂设计

问题：

* 自由排版能力有限
* 图片、装饰元素的位置不够自由
* 很难快速制作视觉上比较个性化的 Bio

---

## 2.2 产品机会

提供一个处于两者之间的工具：

> **不要求用户学习专业设计软件，但允许用户自由摆放自己的内容。**

用户可以使用系统提供的小组件，也可以上传自己的素材。

---

# 三、目标用户

## 3.1 核心用户

喜欢制作个性化个人页面 / Bio / Profile 的个人用户。

典型需求：

* 想制作 Tumblr / Rentry 风格的个人介绍
* 想制作自己的 Profile 长图
* 喜欢使用贴纸、装饰、图片
* 不想学习 Photoshop
* 不喜欢被固定模板限制

---

# 四、核心使用场景

## 场景 1：制作个人 Bio

用户打开编辑器。

添加：

* 头像
* 名字
* 自我介绍
* 兴趣
* 社交账号
* 装饰

然后自由排列。

最后导出 PNG。

---

## 场景 2：制作兴趣 Profile

用户添加：

* 图片
* 文字
* 标签
* 装饰
* 自定义素材

制作成一张长图。

---

## 场景 3：使用自己的素材

用户上传自己喜欢的小图片 / PNG / 装饰。

上传后进入：

> 我的素材

以后可以重复使用。

---

# 五、产品核心原则

## 5.1 简单

用户不需要学习设计软件。

---

## 5.2 自由

元素可以自由拖动和排列。

---

## 5.3 低学习成本

用户看到一个元素就应该知道：

> 可以拖进去。

---

## 5.4 个性化

用户不仅可以使用公共组件，也可以上传自己的素材。

---

## 5.5 不追求专业设计工具的完整性

产品不是 Photoshop / Figma 的替代品。

只解决：

> **快速制作自由排版的个人 Bio。**

---

# 六、V1 产品范围

## 6.1 V1 必须实现

### 画布

* 创建长画布
* 默认画布尺寸
* 设置背景
* 元素自由摆放

### 文字

* 添加文字
* 编辑文字
* 调整字体大小
* 调整字体粗细
* 调整颜色
* 调整对齐
* 拖动
* 缩放
* 删除
* 复制

### 图片

* 本地上传图片
* 添加到画布
* 拖动
* 缩放
* 删除
* 复制
* 圆角
* 透明度

### 元素管理

* 选中
* 取消选中
* 拖动
* 缩放
* 删除
* 复制
* 基础层级控制

### 网格

* 显示 / 隐藏网格
* 网格吸附
* 自由模式
* 网格模式

### 公共组件

提供基础 Bio 装饰：

* Divider
* Sticker
* Label
* Box
* 简单装饰符号

### 我的素材

* 上传素材
* 查看素材
* 使用素材
* 删除素材
* 浏览器本地保存

### 保存

* 自动保存
* 刷新后恢复编辑状态

### 导出

* 导出 PNG

---

# 七、V1 暂不实现

以下功能不属于 V1：

* 用户登录
* 用户注册
* 云端保存
* 多设备同步
* 社区
* 评论
* 点赞
* 分享链接
* 在线协作
* 模板市场
* AI 生成 Bio
* AI 图片生成
* 视频
* 音频
* 多页面网站
* 完整网页生成
* 专业级图片编辑
* 复杂动画
* 在线字体市场

这些功能可以作为未来方向，但不得因为这些未来需求扩大 V1 的复杂度。

---

# 八、信息架构

整体界面分为三个主要区域：

```text
┌────────────────────────────────────────────┐
│                    Header                  │
├──────────────┬────────────────┬────────────┤
│              │                │            │
│  Components  │                │ Properties │
│              │                │            │
│  My Assets   │     Canvas     │            │
│              │                │            │
│              │                │            │
└──────────────┴────────────────┴────────────┘
```

---

# 九、功能详细说明

## 9.1 Canvas

### 功能

提供一个纵向编辑画布。

默认：

```text
Width: 600px
Height: 900px
```

具体尺寸应可配置。

---

### 背景

V1 支持：

* 纯色背景

未来可以增加：

* 图片背景
* 渐变
* 背景素材

---

# 十、Element 元素系统

画布中的内容统一称为：

> Element

Element 至少包含：

```text
id
type
x
y
width
height
rotation
zIndex
content
style
```

---

## 10.1 Element 类型

V1：

```text
text
image
sticker
divider
box
label
```

未来可以增加更多类型。

---

# 十一、文字功能

## 添加

用户点击：

> * Text

创建默认文字。

---

## 编辑

点击文字后可以编辑内容。

---

## 样式

支持：

* Font Size
* Font Weight
* Color
* Text Align

---

## 操作

支持：

* Drag
* Resize
* Delete
* Duplicate

---

# 十二、图片功能

用户点击：

> * Image

选择本地图片。

图片显示在画布中。

支持：

* Drag
* Resize
* Delete
* Duplicate
* Border Radius
* Opacity

图片默认保持宽高比例。

---

# 十三、组件库

左侧提供：

> Components

组件以卡片 / 缩略图形式显示。

例如：

```text
Components

[ Divider ]

[ ♡ ]

[ ✦ ]

[ Label ]

[ Box ]
```

用户可以：

> 点击 / 拖动组件 → 添加到画布。

组件不需要复杂配置。

---

# 十四、我的素材

提供：

> My Assets

用户可以上传自己的图片 / PNG 素材。

---

## 上传流程

```text
Upload
 ↓
选择本地文件
 ↓
保存到浏览器
 ↓
显示在 My Assets
 ↓
拖入画布
```

---

## 本地保存

V1 不使用服务器。

素材保存到浏览器本地。

如果 localStorage 不适合存储图片，则使用 IndexedDB。

---

# 十五、网格系统

## 15.1 自由模式

默认模式。

元素可以自由移动。

---

## 15.2 网格模式

开启后：

* 显示网格
* 元素移动自动吸附

默认：

```text
Grid Size = 8px
```

网格尺寸应该可配置。

---

## 15.3 Grid UI

建议：

```text
Grid   OFF / ON
Snap   OFF / ON
```

不要强迫用户理解复杂的网格参数。

---

# 十六、元素拖动

用户按住元素并移动鼠标。

元素跟随鼠标移动。

松开后确定位置。

---

## 网格模式下

元素坐标自动吸附到最近网格点。

例如：

```text
x = 137
```

8px 网格：

```text
x = 136
```

---

# 十七、元素缩放

选中元素后显示 resize handles。

至少支持四角：

```text
┌──────────────┐
│              │
│    Element   │
│              │
└──────────────┘
```

图片默认保持比例。

---

# 十八、选中状态

点击元素：

> 显示选中框。

点击画布空白区域：

> 取消选择。

---

# 十九、属性面板

右侧显示当前选中元素的属性。

不同类型显示不同属性。

---

## Text

```text
Content
Font Size
Font Weight
Color
Align
```

---

## Image

```text
Width
Height
Radius
Opacity
```

---

## Sticker / Component

只显示必要属性。

---

# 二十、删除与复制

支持：

```text
Delete
Backspace
```

删除当前元素。

支持：

```text
Duplicate
```

复制元素。

如果实现方便，可以增加：

```text
Ctrl/Cmd + C
Ctrl/Cmd + V
```

---

# 二十一、保存

V1 不需要用户账号。

保存：

```text
Canvas
Elements
Grid Settings
Assets
```

刷新页面后恢复。

---

# 二十二、导出

用户点击：

> Export PNG

将整个画布导出为 PNG。

导出的内容应尽可能与编辑器中的画布视觉效果一致。

---

# 二十三、用户流程

## 新用户第一次打开

```text
打开编辑器
 ↓
看到空白画布
 ↓
左侧看到 Components
 ↓
点击 Text
 ↓
输入自己的内容
 ↓
添加图片
 ↓
拖动调整位置
 ↓
添加装饰
 ↓
打开 Grid / Snap
 ↓
调整排版
 ↓
Export PNG
```

不需要教程。

界面本身应该足够直观。

---

# 二十四、错误处理

至少处理：

### 图片上传失败

提示：

> Unable to load this image.

### 不支持的文件

提示：

> Unsupported file type.

### 本地存储失败

提示：

> Unable to save locally.

不能因为一个图片加载失败导致整个编辑器崩溃。

---

# 二十五、性能要求

这是一个前端个人编辑器。

需要避免：

* 每次 mousemove 都触发大量 DOM 重建
* 重复渲染整个画布
* 重复读取大型图片
* 大量不必要的事件监听器

拖动元素时应该保持流畅。

---

# 二十六、可维护性要求

代码需要保持模块化。

建议：

```text
state
canvas
elements
drag
resize
grid
assets
storage
export
```

分别承担不同职责。

不要把所有逻辑放进：

```text
index.html
```

或者单个巨型 JS 文件。

---

# 二十七、数据原则

State 应作为主要数据来源。

例如：

```js
state = {
  canvas: {},
  elements: [],
  selectedElementId: null,
  grid: {},
  assets: []
}
```

DOM 和 State 必须保持一致。

不要出现：

> DOM 显示 A，但是 State 保存的是 B。

---

# 二十八、非功能需求

## 易用性

用户第一次打开页面应该可以直接开始编辑。

---

## 性能

常规数量的元素下拖动应该流畅。

---

## 稳定性

单个元素出错不能导致整个编辑器崩溃。

---

## 可维护性

新增 Element 类型时，不应该需要修改大量无关代码。

---

# 二十九、V1 验收标准

V1 完成至少满足：

### Canvas

* [ ] 能看到画布
* [ ] 能修改背景
* [ ] 元素可以放入画布

### Text

* [ ] 可以添加
* [ ] 可以编辑
* [ ] 可以拖动
* [ ] 可以缩放
* [ ] 可以删除
* [ ] 可以复制
* [ ] 可以调整基本样式

### Image

* [ ] 可以上传
* [ ] 可以显示
* [ ] 可以拖动
* [ ] 可以缩放
* [ ] 可以删除
* [ ] 可以复制

### Grid

* [ ] 可以显示网格
* [ ] 可以隐藏网格
* [ ] Snap 可以开启
* [ ] Snap 可以关闭
* [ ] 开启 Snap 后元素可以正确吸附

### Assets

* [ ] 可以上传
* [ ] 可以显示
* [ ] 可以拖入画布
* [ ] 可以删除
* [ ] 刷新后仍然存在

### Storage

* [ ] 编辑内容可以保存
* [ ] 刷新后可以恢复

### Export

* [ ] 可以导出 PNG

---

# 三十、未来方向

V1 完成后再考虑：

```text
Smart Guides
Undo / Redo
Templates
More Components
More Asset Types
Background Image
Gradient
Text Effects
Rotation
Multi-select
Group
Alignment
JSON Import / Export
Shareable Links
Cloud Storage
```

这些全部属于后续版本。

---

# 三十一、产品最终目标

这个产品最终希望让用户感觉：

> “我不是在学一个设计软件。”

而是：

> “我只是把我喜欢的东西拖进去，然后摆成我喜欢的样子。”

核心体验：

```text
＋ Text
＋ Image
＋ Component
＋ My Asset

        ↓

      Drag

        ↓

      Arrange

        ↓

      Export
```

**简单，但不受模板限制。**

**自由，但不需要专业设计知识。**
